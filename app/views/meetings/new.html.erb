<div class="container-forms d-flex justify-content-center text-center mt-4">
  <div class="row">
    <h3>Creating a new routine<h3>
    <p id="landing-info">Fill up the form bellow to book one of your Trails in your calendar!</p3>
  </div>
</div>

<% unless @trails.present? %>
    <h3>You don't have any Trails yet ):</h3>
    <%= link_to "Create new Trail", new_trail_path, class: "btn btn-info" %>
    <%else %>
<%= render 'form', meeting: @meeting %>
<%end %>

<div class="container-forms d-flex justify-content-center text-center mt-5">
  <%= link_to 'Go back!', meetings_path, class: 'btn btn-primary' %>
</div>

<%= flash[:alert] if flash[:alert].present? %>
